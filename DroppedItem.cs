using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//simply a class that is a way to instantiate Item, which is not a monobehaviour. (this allows items to be made with constructors)
public class DroppedItem : MonoBehaviour {

	public Item item; //this is the item that the droppedItem represents

	// Use this for initialization
	void Start () {

	}

	// Update is called once per frame
	void Update () {

	}
	
}
